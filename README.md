Create bank_management.sql
CREATE DATABASE → CREATE TABLE → PRIMARY KEY → FOREIGN KEY → INSERT → SELECT → WHERE → JOIN → SUM → MAX

--------It manages::--------------

Customers
Bank accounts
Transactions
Deposits
Withdrawals
Account balances
